{
  "name": "modelscope-mcp",
  "version": "1.0.0",
  "description": "MCP server (ContainerAgent) that proxies container actions (execute/read/write) to a remote agent API",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node test-client.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.1.13"
  }
}
